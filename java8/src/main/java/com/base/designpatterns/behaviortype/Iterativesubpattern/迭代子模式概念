迭代子模式
    又叫游标模式，是对象的行为模式
    定义：可以顺序地访问一个聚集中的元素而不必暴露集的内部对象

    角色：
        迭代器角色（iterator）:负责定义访问和遍历元素的接口
        具体迭代角色（concreteIterator）:实现迭代器接口，并要记录遍历中的当前位置
        容器角色（container）:负责提供创建具体迭代器角色的接口
        具体容器接口（concreteContainer）:实现创建具体迭代器角色的接口，这个具体迭代器角色与容器的结构相关
