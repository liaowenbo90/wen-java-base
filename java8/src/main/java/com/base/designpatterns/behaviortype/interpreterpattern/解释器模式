解释器模式
    定义：给定一种语言，定义他的文法的一种表示，并定义一个解释器，该解释器使用该表示来解释语言中句子
    角色：
        抽象解释器(expression)：声明一个所有具体表达式都要实现的抽象接口，接口中主要是一个interpret方法，称为解释操作
        终结符表达式(terminal expression)：实现与文法跌元素相关的解释操作
        非终结符表达式(nonterminal expression)：文法中的每条规则对应一个非张结符表达式
        环境角色(context)：这个角色的任务一般是用来存文法中各个终结所对应的具体值
    优点：
        是一个简单的语法分析工具，它最易著的优点就是扩展性，修复习计划语法规则只需修改相应的非终结符就可以了
    适用场景：
        有一个简单的语法规则，如sql ,进行rm转换，就可以解释器进行解释
        一些重复发生的问题
